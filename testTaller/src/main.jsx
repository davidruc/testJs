import React from 'react'
import ReactDOM from 'react-dom/client'
import { program } from '../Version/V1/exercise10'

program();

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
